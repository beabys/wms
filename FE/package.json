{
  "name": "wms-frontend",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "storybook": "pnpm --filter @wms/ui storybook",
    "test": "pnpm -r test"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  }
}
