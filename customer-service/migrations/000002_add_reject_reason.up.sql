ALTER TABLE customers ADD COLUMN IF NOT EXISTS reject_reason TEXT;

-- Update updated_at trigger if not exists
CREATE OR REPLACE FUNCTION update_updated_at_column()
RETURNS TRIGGER AS $$
BEGIN
    NEW.updated_at = NOW();
    RETURN NEW;
END;
$$ language 'plpgsql';
