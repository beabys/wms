service:
  name: "inbound-service"
  env: "development"

grpc:
  port: 50003

database:
  dsn: "postgres://wms:wms_local_dev@localhost:5432/wms_inbound?sslmode=disable"

kafka:
  brokers: []

auth:
  jwks_url: "http://localhost:8080"
  exempt_methods:
    - "/grpc.health.v1.Health/Check"

jwt_public_key_path: "login-service/keys/public.pem"
