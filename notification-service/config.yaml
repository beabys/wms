service:
  name: "notification-service"
  env: "development"
grpc:
  port: 50011
sender:
  type: "console"      # console | smtp (smtp is stub for now)
smtp:
  host: ""
  port: 587
  username: ""
  password: ""
auth:
  api_key: "dev-api-key-notification"
  exempt_methods:
    - "/grpc.health.v1.Health/Check"
